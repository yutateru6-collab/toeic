# 問題の編集・校閲・更新

初回90問に、手元資料240問の分析を参考にした90問を追加し、現在180問。追加問題は別のAIによる全択レビューと修正後の再確認を行いました。独立した専門家の承認済み教材ではありません。自動テストが文法的正確さや本番難易度を保証することはありません。

1. `src/questions.ts`の1行が1問。区切りは`|`、選択肢・理由の区切りは`~`。配列内の順序でIDが決まるため、既存行の並びを変更しない。
2. 正解番号は原稿の0始まりの番号。画面では固定の回転を行い、選択肢と理由を同時に移動する。
3. 修正時は公開後の問題versionを上げる。大幅な論点変更は新しい問題IDとして扱い、以前の成績との比較を行わない。
4. 問題文に4候補をすべて入れて解き直し、辞書・語法・意味で各誤答が不適切な根拠を確認する。別解・自然な読み方が残った場合は公開問題から外し、件数表示とテストも更新する。
5. 日本語訳と解説の整合性を確認。文法上入りうるが文脈に合わない語を「文法的に絶対不可」と説明しない。
6. 語数・構文・選択肢型・主論点・判断範囲を公式資料の分析と照合する。難語を増やすだけで難問にしない。
7. `pnpm test`と`pnpm build`を通し、スマホで当該問と解説を読む。
8. Gitの変更をレビューし、`main`に反映。Cloudflareのビルドと公開結果を確認する。

追加問題は`src/expandedQuestions.ts`にあり、各行は論点・英文・選択肢・訳・着目箇所・判断基準・選択肢理由の7列。原稿の正答は先頭、表示時に理由と一緒に固定回転する。各分野の前3分の2が練習、後3分の1がセット02用。既存IDと行順を変更しない。`getExamSet`のセット02順序は分野配分に対応するため、配分を変更する際は順序とテストも更新する。

現行familyは各問ID。出題論点の一致と、実質的に同じ問題の一致は区別する。今後テンプレート類題を追加する場合は同一familyを付け、練習と評価をまたがせない。固定30問×2セットによる成績は標準化テストではない。

## 本番レベルを検証する次工程

手元の8回分240問の本文分析・正解照合は完了。出典範囲と方法は[詳細分析](local-corpus-analysis.md)を参照。今後は表紙・奥付による書誌確認、専門家校閲、初見回答者の正答率・選択肢選択率・時間と適切な基準課題の結果の比較に進む。確認数と対象集団を明記し、実測値を推測で埋めない。元PDF・OCR・本文転記は公開しない。
